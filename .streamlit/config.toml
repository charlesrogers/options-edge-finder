[server]
headless = true

[theme]
# House design system — light mode (Stripe-inspired)
# From style-guide repo (oklch → hex)
base = "light"
primaryColor = "#6344df"
backgroundColor = "#fafbfc"
secondaryBackgroundColor = "#f0f1f5"
textColor = "#1a1d2e"
font = "sans serif"
